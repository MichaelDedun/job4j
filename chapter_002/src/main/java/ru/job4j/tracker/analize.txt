Использовал для анализа jconsole
При запуске программы я увидел что используется 20mb хипа,
спустя минуту сработал gc и отчистил память после чего стало использоваться 5mb в heap,
при этом в perm jeneration (non heap) значение не изменялось и было всегда равно 1.2mb
т.к там хранятся имьтбл значения (String и сигнатуры классов и тд)
после того как я начал вызывать методы такие как:
-показать все заявки;
- создать заявку;
после чего загруженность heap возрасла до 12mb, после небольшой паузы сработал gc и значение heap стало равно 4.5mb
Для вызова out of memory использовал такой кусок кода в мейне
List<Object[]> arrays = new LinkedList<>();
for (; ; ) {
      arrays.add(new Object[100]);
    }
после чего поймал outofmemoryerror и увидел переполнение кучи.
